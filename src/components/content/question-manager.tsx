"use client";
import Link from 'next/link'; import { useMemo, useState } from 'react'; import { Copy, Eye, Pencil, Plus, Trash2 } from 'lucide-react';
import { deleteQuestion, duplicateQuestion } from '@/lib/content/actions'; import type { Category, Question } from '@/lib/content/types'; import { Button } from '@/components/ui/button'; import { Input } from '@/components/ui/input'; import { EmptyState } from '@/components/ui/empty-state'; import { Dialog } from '@/components/ui/dialog';
export function QuestionManager({ questions, categories }: { questions: Question[]; categories: Category[] }) { const [search, setSearch] = useState(''); const [category, setCategory] = useState(''); const [type, setType] = useState(''); const [difficulty, setDifficulty] = useState(''); const [status, setStatus] = useState(''); const [preview, setPreview] = useState<Question | null>(null); const filtered = useMemo(() => questions.filter((q) => q.title.toLowerCase().includes(search.toLowerCase()) && (!category || q.category_id === category) && (!type || q.question_type === type) && (!difficulty || q.difficulty === Number(difficulty)) && (!status || q.status === status)), [questions, search, category, type, difficulty, status]); return <div className="space-y-6"><div className="flex flex-col gap-3 lg:flex-row"><Input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Search questions" aria-label="Search questions" /><select value={category} onChange={(e) => setCategory(e.target.value)} className="h-11 rounded-lg border bg-white px-3 text-sm"><option value="">All categories</option>{categories.map(c => <option key={c.id} value={c.id}>{c.name}</option>)}</select><select value={type} onChange={(e) => setType(e.target.value)} className="h-11 rounded-lg border bg-white px-3 text-sm"><option value="">All types</option><option value="multiple_choice">Multiple choice</option><option value="true_false">True / False</option></select><select value={difficulty} onChange={(e) => setDifficulty(e.target.value)} className="h-11 rounded-lg border bg-white px-3 text-sm"><option value="">All difficulties</option>{[1,2,3,4,5].map(n => <option key={n} value={n}>Difficulty {n}</option>)}</select><select value={status} onChange={(e) => setStatus(e.target.value)} className="h-11 rounded-lg border bg-white px-3 text-sm"><option value="">All statuses</option><option value="draft">Draft</option><option value="published">Published</option><option value="archived">Archived</option></select><Link href="/questions/new"><Button><Plus className="size-4" />New question</Button></Link></div>{filtered.length ? <div className="overflow-x-auto rounded-2xl border bg-white"><table className="w-full min-w-[760px] text-left text-sm"><thead className="border-b bg-slate-50 text-slate-500"><tr><th className="p-4">Question</th><th>Category</th><th>Type</th><th>Difficulty</th><th>Status</th><th className="p-4">Actions</th></tr></thead><tbody className="divide-y">{filtered.map(q => <tr key={q.id}><td className="max-w-sm p-4 font-semibold">{q.title}</td><td>{q.categories?.name}</td><td>{q.question_type === 'true_false' ? 'True / False' : 'Multiple choice'}</td><td>{'●'.repeat(q.difficulty)}</td><td className="capitalize">{q.status}</td><td className="p-4"><div className="flex"><Button variant="ghost" size="sm" onClick={() => setPreview(q)} aria-label="Preview"><Eye className="size-4" /></Button><Link href={`/questions/${q.id}`}><Button variant="ghost" size="sm" aria-label="Edit"><Pencil className="size-4" /></Button></Link><Button variant="ghost" size="sm" onClick={() => duplicateQuestion(q.id)} aria-label="Duplicate"><Copy className="size-4" /></Button><Button variant="ghost" size="sm" onClick={() => { if (confirm('Delete this question?')) deleteQuestion(q.id); }} aria-label="Delete"><Trash2 className="size-4 text-red-600" /></Button></div></td></tr>)}</tbody></table></div> : <EmptyState title={questions.length ? 'No questions match' : 'Question bank is empty'} description="Create a reusable question to begin building your game content." /> }<Dialog open={preview !== null} onClose={() => setPreview(null)} title="Question preview">{preview && <div className="space-y-4"><p className="text-lg font-bold">{preview.title}</p>{preview.media?.signedUrl && (preview.media.type === 'image' ? <img src={preview.media.signedUrl} alt="Question media" className="max-h-56 w-full rounded-xl object-cover" /> : <video src={preview.media.signedUrl} controls className="w-full rounded-xl" />)}<div className="space-y-2">{preview.answers.map(a => <p className={`rounded-lg border p-3 ${a.is_correct ? 'border-green-300 bg-green-50' : ''}`} key={a.display_order}>{a.label}</p>)}</div></div>}</Dialog></div>; }
