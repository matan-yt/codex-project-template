"use client";
import Link from "next/link";
import { Hexagon, X } from "lucide-react";
import { usePathname } from "next/navigation";
import { cn } from "@/lib/utils/cn";
import { navItems } from "./nav-items";
export function Sidebar({ open, onClose }: { open: boolean; onClose: () => void }) { const pathname = usePathname(); return <aside className={cn("fixed inset-y-0 left-0 z-40 flex w-72 flex-col border-r border-hive-900/10 bg-hive-950 px-4 py-5 text-white transition-transform lg:static lg:translate-x-0", open ? "translate-x-0" : "-translate-x-full")}><div className="flex items-center justify-between px-3"><Link href="/" className="flex items-center gap-2 text-xl font-extrabold"><span className="grid size-9 place-items-center rounded-xl bg-honey-400 text-hive-950"><Hexagon className="size-5 fill-current" /></span>HivePlay</Link><button onClick={onClose} className="rounded-lg p-2 hover:bg-white/10 lg:hidden" aria-label="Close navigation"><X /></button></div><nav className="mt-10 space-y-1" aria-label="Main navigation">{navItems.map(({ href, label, icon: Icon }) => { const active = href === "/" ? pathname === "/" : pathname.startsWith(href); return <Link onClick={onClose} key={href} href={href} className={cn("flex items-center gap-3 rounded-xl px-3 py-3 text-sm font-semibold transition-colors", active ? "bg-honey-400 text-hive-950" : "text-hive-100 hover:bg-white/10 hover:text-white")}><Icon className={cn("size-5", active && "fill-current")} />{label}</Link>; })}</nav><p className="mt-auto px-3 text-xs text-hive-300">HivePlay foundation · v0.1</p></aside>; }
