import { ArrowRight, CalendarClock, Gamepad2, Sparkles } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent, CardHeader } from "@/components/ui/card";
import { EmptyState } from "@/components/ui/empty-state";
import { PageHeader } from "@/components/ui/page-header";

const metrics = [["Games", "—", Gamepad2], ["Questions", "—", Sparkles], ["Sessions", "—", CalendarClock]];
export default function DashboardPage() { return <div className="space-y-8"><PageHeader title="Dashboard" description="Your HivePlay workspace is ready. Start building content when the next phase begins." /><section className="grid gap-4 sm:grid-cols-3">{metrics.map(([label, value, Icon]) => { const MetricIcon = Icon as typeof Gamepad2; return <Card key={label as string}><CardContent className="flex items-start justify-between"><div><p className="text-sm font-semibold text-slate-500">{label as string}</p><p className="mt-3 text-3xl font-extrabold">{value as string}</p><Badge className="mt-3">No data yet</Badge></div><MetricIcon className="size-5 text-honey-700" /></CardContent></Card>; })}</section><section className="grid gap-6 xl:grid-cols-2"><Card><CardHeader><h2 className="font-bold">Recent games</h2><p className="mt-1 text-sm text-slate-500">Game templates will appear here.</p></CardHeader><CardContent><EmptyState title="No games yet" description="Games are not available in the foundation phase." /></CardContent></Card><Card><CardHeader><h2 className="font-bold">Recent sessions</h2><p className="mt-1 text-sm text-slate-500">Live activity will appear here.</p></CardHeader><CardContent><EmptyState title="No sessions yet" description="Sessions are not available in the foundation phase." /></CardContent></Card></section><Card className="border-honey-400/50 bg-honey-100/40"><CardContent className="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><h2 className="font-bold text-hive-950">Quick actions</h2><p className="mt-1 text-sm text-hive-900">Content, game setup, and sessions will become available in upcoming phases.</p></div><span className="inline-flex items-center gap-2 text-sm font-bold text-honey-900">Foundation complete <ArrowRight className="size-4" /></span></CardContent></Card></div>; }
