import { notFound } from 'next/navigation'; import { QuestionBuilder } from '@/components/content/question-builder'; import { PageHeader } from '@/components/ui/page-header'; import { getCategories, getMedia, getQuestion } from '@/lib/content/queries';
export default async function EditQuestionPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const [question, categories, media] = await Promise.all([getQuestion(id), getCategories(), getMedia()]); if (!question) notFound(); return <div className="space-y-8"><PageHeader title="Edit question" description="Changes affect this reusable content only." /><QuestionBuilder question={question} categories={categories} media={media} /></div>; }
