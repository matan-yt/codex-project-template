import { notFound } from "next/navigation";
import { EmptyState } from "@/components/ui/empty-state";
import { PageHeader } from "@/components/ui/page-header";
const sections: Record<string, { title: string; description: string }> = { games: { title: "Games", description: "Build reusable game templates." }, questions: { title: "Questions", description: "Manage reusable question content." }, categories: { title: "Categories", description: "Organize your question bank." }, media: { title: "Media", description: "Manage images and video assets." }, sessions: { title: "Sessions", description: "Run and review live games." }, settings: { title: "Settings", description: "Manage your HivePlay workspace." } };
export default async function PlaceholderPage({ params }: { params: Promise<{ section: string }> }) { const { section } = await params; const page = sections[section]; if (!page) notFound(); return <div className="space-y-8"><PageHeader title={page.title} description={page.description} /><EmptyState title={`${page.title} are coming soon`} description={`This area is intentionally reserved for the next HivePlay phase. No ${page.title.toLowerCase()} data exists yet.`} /></div>; }
