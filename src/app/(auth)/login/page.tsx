import Link from "next/link";
import { Hexagon } from "lucide-react";
import { signIn } from "@/lib/auth/actions";
import { Alert } from "@/components/ui/alert";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
export default async function LoginPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) { const { error } = await searchParams; return <main className="hex-grid grid min-h-screen place-items-center bg-hive-950 p-5"><section className="w-full max-w-md rounded-3xl border border-white/10 bg-white p-7 shadow-2xl sm:p-9"><Link href="/login" className="flex items-center gap-3 text-xl font-extrabold text-hive-950"><span className="grid size-10 place-items-center rounded-xl bg-honey-400"><Hexagon className="size-6 fill-current" /></span>HivePlay</Link><div className="mt-10"><p className="text-xs font-bold uppercase tracking-[.16em] text-honey-700">Admin sign in</p><h1 className="mt-2 text-3xl font-extrabold tracking-tight">Welcome back</h1><p className="mt-3 text-slate-600">Sign in to manage your game content and live events.</p></div>{error && <Alert variant="error" className="mt-6">{error}</Alert>}<form action={signIn} className="mt-7 space-y-5"><div className="space-y-2"><Label htmlFor="email">Email</Label><Input id="email" name="email" type="email" autoComplete="email" required placeholder="you@example.com" /></div><div className="space-y-2"><Label htmlFor="password">Password</Label><Input id="password" name="password" type="password" autoComplete="current-password" required /></div><Button className="w-full" type="submit">Sign in to HivePlay</Button></form><p className="mt-6 text-center text-sm text-slate-500">Accounts are created by an administrator.</p></section></main>; }
